<!-- THIS IS A Bootstrap TEMPLATE THAT I MODIFIED, I DO NOT OWN THIS TEMPLATE -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Candidate</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h1>Delete a Candidate</h1>
        <% if (message) { %>
            <div class="alert alert-info" role="alert">
                <%= message %>
            </div>
            <% } %>
                <form action="/admin/delete" method="POST">
                    <div class="form-group">
                        <label for="candidateToDelete">Select Candidate to Delete</label>
                        <select class="custom-select" id="candidateToDelete" name="candidateID">
                            <option selected disabled>Select a candidate to delete</option>
                            <% candidates.forEach(candidate=> { %>
                                <option value="<%= candidate._id %>">
                                    <%= candidate.name %>
                                </option>
                                <% }); %>
                        </select>
                    </div>
                    <button class="btn btn-danger" type="submit">Delete Candidate</button>
                </form>
    </div>
</body>

</html>