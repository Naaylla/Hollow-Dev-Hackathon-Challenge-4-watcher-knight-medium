<!-- THIS IS A Bootstrap TEMPLATE THAT I MODIFIED, I DO NOT OWN THIS TEMPLATE -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            position: relative;
            min-height: 100vh;
            margin: 0;
            background-color: #f8f9fa;
        }

        .admin-page {
            width: 100%;
            min-height: 100vh;
            position: relative;
            padding: 2rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }

        h1 {
            font-weight: bold;
            color: #007bff;
            margin-bottom: 1.5rem;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .top-left {
            position: absolute;
            top: 20px;
            left: 20px;
        }

        .top-right {
            position: absolute;
            top: 20px;
            right: 20px;
        }

        .admin-options {
            margin-top: 5rem;
        }
    </style>
</head>

<body>
    <div class="admin-page">
        <div class="top-left">
            <h1>Welcome, admin <%= user.name %>!</h1>
            <% if (message) { %>
                <div class="alert alert-info" role="alert">
                    <%= message %>
                </div>
                <% } %>
        </div>
        <form action="/logout?_method=DELETE" method="POST" name="logout">
            <div class="top-right">
                <button class="btn btn-primary text-white" type="submit">Log out</button>
            </div>
        </form>

        <!-- Admin Options -->
        <div class="admin-options">
            <h2>Admin Options</h2>

            <!-- Navigation Links -->
            <ul class="list-unstyled">
                <li><a href="/admin/add">Add a New Candidate</a></li>
                <li><a href="/admin/update">Update a Candidate</a></li>
                <li><a href="/admin/delete">Delete a Candidate</a></li>
                <li><a href="/admin/votes">Check Votes</a></li>
            </ul>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
</body>

</html>